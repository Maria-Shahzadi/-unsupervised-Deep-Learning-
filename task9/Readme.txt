CNN Architecture Exploration: Build a CNN in Keras on the MNIST or CIFAR-10
dataset with at least 2 Conv+Pool blocks followed by Dense layers. After training, extract
and visualise the learned filters from the first Conv layer. Also visualise the feature maps
(intermediate activations) produced by each Conv layer for a single sample image.
Compare CNN test accuracy vs the Day 7 FNN on the same dataset. Plot and compare
training curves side by side. Print the total parameter counts for both architectures and
write an explanation of why CNNs use far fewer parameters than a comparable Dense
network.