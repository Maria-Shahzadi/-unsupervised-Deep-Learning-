DBSCAN on Complex Shapes: Generate non-spherical synthetic datasets using
make_moons and make_circles from sklearn. Apply K-Means, Agglomerative Clustering,
and DBSCAN to all datasets. Visualise all results side by side in a grid to show where
each algorithm succeeds and fails. Plot the K-Distance Graph to justify your epsilon
choice. Print Silhouette Scores for valid clustering results and produce a final
comparison table of all three algorithms across all datasets with a written conclusion.