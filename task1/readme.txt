K-Means from Scratch + sklearn: Using the Iris dataset or a synthetic 2D dataset
(make_blobs), implement K-Means manually using NumPy — initialise centroids
randomly, assign each point to the nearest centroid, recompute centroids, and repeat
until convergence. Then repeat the same using sklearn's KMeans(). Visualise clusters
with a scatter plot, marking centroids clearly. Plot the Elbow curve for K = 1 to 10.
Compare manual vs sklearn cluster assignments and print Silhouette Scores for both.