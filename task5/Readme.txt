Perceptron from Scratch: Implement a Perceptron class in pure Python and NumPy
without any ML library. Train it on linearly separable datasets (AND gate, OR gate).
Visualise the decision boundary updating after each epoch as an animation or series of
plots. Then attempt to train it on the XOR dataset — show and explain why it fails to
converge. Plot the error curve over epochs for both cases. Document clearly why XOR
fails and what architectural change would fix it.